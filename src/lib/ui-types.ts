export type InputMode = "single" | "four" | "six";
export type ViewKey = "front" | "left" | "right" | "back" | "top" | "bottom";
export type Stage = "idle" | "queued" | "running" | "succeeded" | "failed";
export type ImageDimensions = { width: number; height: number };
